{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "complie",			
            "type": "shell",
            "command": "g++",
            "args": [
                "--std=c++11",
                "-g",
                "${fileDirname}/${fileBasename}",
                "-o2",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}.out"
            ],
            "presentation": {
                "reveal": "silent"
            },
            "problemMatcher": "$gcc"
        }
    ]
}
